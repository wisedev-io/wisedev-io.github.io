/**
 * Contact API endpoint.
 * Point this at your deployed Flask backend (see /backend folder).
 * Leave as-is until the backend is live — the form falls back to email automatically.
 *
 * NOTE: temporarily pointed at the local dev backend (see /backend, running on :5050)
 * for local testing. Switch back to https://api.kamoliddin.is-a.dev/api/contact
 * once that backend is actually deployed and live, before deploying the frontend.
 */
export const CONTACT_API_URL = 'http://localhost:5050/api/contact';

export const EMAIL = 'kamoliddin@srpsoftware.com';
export const PHONE = '+998 93 752 9804';
export const GITHUB = 'https://github.com/wisedev-io';
export const LINKEDIN = 'https://linkedin.com/in/kamoliddin-hr';
